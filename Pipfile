[[source]]
url = "https://pypi.python.org/simple"
verify_ssl = true
name = "pypi"

[dev-packages]
black = "==18.9b0"
django-apiblueprint-view = ">=2.0.0,<3.0"
pytest = ">=4.0.1,<5.0"
pytest-django = ">=3.4.4,<4.0"
pytest-flakes = ">=4.0.0,<5.0"

[packages]
django = ">=1.11,<2.0"
aiohttp = ">=3.4"
dc-base-theme = {editable = true,git = "https://github.com/DemocracyClub/dc_base_theme.git",ref = "0.3.7"}
django-storages = "==1.6.5"

[requires]
python_version = "3.6"

[scripts]
black = "black ."
black-check = "black --check ."
circleci_deploy = "~/repo/deploy.sh"
pytest = "pytest"
pytest-flake = "pytest --flake"
